[pytest]
testpaths = tests
log_cli = true
log_cli_level = INFO

markers =
    integration: marks tests as integration tests requiring a live SQL Server connection (deselect with -m "not integration")
