[pytest]
testpaths = tests
log_cli = true
log_cli_level = INFO
addopts = -m "not integration"

markers =
    integration: marks tests as integration tests requiring a live SQL Server connection (run with: pytest -m integration)
